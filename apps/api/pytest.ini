[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:pytest_asyncio.*
    ignore:cannot collect 'test_session_factory'.*:pytest.PytestCollectionWarning
